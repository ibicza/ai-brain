# M-33.6c performance report

Exact-I18 Windows and Karina measurements are written under `runs/m336c_final_gate/<platform>/`. The typed performance report covers frozen SPDX snapshot load, template matching, evidence fusion, document-role classification, entry eligibility, candidate qualification, artifact-contract lookup/validation/disclosure extraction, complete disclosed provenance, Java production, candidate-pack compilation/replay and independent evaluator.

Repeated micro-operations report p50, p95, p99 and throughput. Single end-to-end operations report the measured sample as all three percentiles, alongside target throughput and peak Python memory. Platform time and memory are descriptive and are not required to be byte-identical; semantic and contract artifacts are.
