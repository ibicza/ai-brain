# M-33.6d security report

Pre-freeze authority attacks accepted zero forged, widened, cross-source, cross-run, cross-F19, or cross-vault receipts. The adaptive battery rejected 10,260/10,260 internally consistent mutations at the expected layer, and H17 mapped 36/36 field occurrences. The final leak scan found zero fresh-source leaks.

Post-freeze fail-closed findings are preserved rather than repaired: the public artifact contract rejects an acquisition report produced by its own pipeline; the vault verifier is path-order dependent; and exact H19 quality rejects the append-only disclosure registry through a frozen historical cardinality assertion.

No moral filter, moderation rule, refusal policy, political or ideological restriction, topic ban, personality control, or generic harmful-content classifier was added. Source-use rules remain byte-retention and publication contracts only.
