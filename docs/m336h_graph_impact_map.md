# M-33.6h graph impact map

The graph-first inspection started from F22 with 14,368 nodes, 104,642 edges, and 1,070 files. After the native-route implementation the rebuilt graph contains 14,476 nodes, 106,013 edges, and 1,081 files.

The new controller calls compiler-aware production directly. Production reaches strict M336F deserialization, compilation-closure verification, JDK verification, sealed replay, and public-pack verification. It does not reach the independent evaluator or golden material. The final route does not call the M336E selector or the disclosed M336g wrapper. The evaluator imports only canonical hashing, public-pack contracts, and M336H contracts.

The exact machine-readable graph queries and zero-denominator checks are emitted in `artifacts/m336h/graph_impact.json` during Q23 qualification.
