# M-30 progress projection policy

`NOT_SEEN` means no trusted grade. `PRACTICING` means at least one. `DEMONSTRATED` requires two correct attempts on distinct semantic keys, no prior solution and at most one hint for each. Two later incorrect trusted attempts on distinct keys produce `NEEDS_REVIEW`. Reset starts a new projection epoch. There is no decay or hidden score.
