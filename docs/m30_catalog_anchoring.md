# M-30 catalog anchoring

Every v4 exercise and tutor session carries an exact catalog-entry anchor. The anchor commits to semantic key, spec, canonical base instance, graph, receipt, source result and answer semantics. Runtime variants may change only deterministic presentation fields; service load resolves the anchor through the loaded trusted catalog and compares the complete authority closure.
