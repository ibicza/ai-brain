# M-33.6f limitations

R21 establishes implementation and test closure, not a release outcome. Development results cannot authorize Q21. Four production diagnostics in the development selected set map to a known source unit but zero declaration IDs; they remain visible and governed by deterministic ambient/source-unit policy rather than being discarded.

Freshness, cache exclusion, metadata pool, one final acquisition, final registry append, final vault equality, final production and E21 evaluation have not begun. Until exact-R21 Windows/Karina qualification passes, the only honest roadmap result is pending/blocked at R21.
