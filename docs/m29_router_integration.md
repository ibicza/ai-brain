# M-29 Router Integration

The controlled RU/EN frontend recognizes explain, generate exercise, check answer, hint, and show solution requests. Parsed routes retain parser evidence and use only the chemistry educational adapter.

Educational routing cannot bypass exact tool/fact authority, explicit schemas, current-domain verification, or tutor-session binding. Unsupported text remains `UNSUPPORTED`; it is not interpreted as a hidden command.
