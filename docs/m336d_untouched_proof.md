# M-33.6d untouched proof

Pre-F19 source-body bytes received and read were both zero. The final acquisition ran globally once after F19. The selector sentinel binds the sealed-vault manifest hash `7cca94dee7c7319097678e32cd5d9d974d83fabfbb9fd6e872737f7687d8dad1`; selector invocation count is one and rerun count is zero.

All 24 acquired roots have zero overlap in each of 12 identity classes: archive hash, canonical source hash, coordinate, correspondence hash, declaration fingerprint, family, POM hash, raw source hash, SCM revision, selected-path manifest hash, source-tree hash, and source URL. The requested minimum 11-class proof is therefore satisfied by the broader 12-class census.

The acquisition failed closed before selecting files. No replacement candidate and no second selector run were attempted.
