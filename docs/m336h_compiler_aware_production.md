# M-33.6h compiler-aware production

`M336HCompilerAwareProductionRequest` makes javac, source-entry bindings, the exact M336F selected manifest and receipt, closure manifest and proof, sealed vault, private replay root, private materialized snapshot, public JDK identity, publication-boundary identity, threshold identity, route manifest identity, and implementation identity mandatory.

Validation occurs before ingestion. Missing or incompatible fields fail closed; the current route cannot silently enter a legacy optional mode. The generic worker derives proposal/trust/withheld counts from the run and contains no disclosed 1587/1575/12 success condition. Production also verifies the source-free candidate pack and performs sealed deterministic replay before returning a public-safe receipt.
