# M-29.1 limitations

- Chemistry scope remains bounded.
- Runtime uses a precompiled catalog where possible; a new calculation still requires normal confirmation.
- Deterministic graph-bound explanations are trusted; free-text interpretation is assistive.
- Diagnosis is pattern-based and conservative. Independent recall is limited, so diagnosis and targeted hints are released only as the safe conservative subset.
- There is no learner personality inference and no unrestricted chemistry.
- Runtime network is disabled; optional neural surface realization is DISABLED_NOT_EVALUATED.
- There are no automatic FactMemory or RuleMemory writes.
- V1 educational sessions are preserved and require explicit archival export; they are not silently opened as v2.
- No moral, moderation, refusal, political, ideological or topic policy was added.

These limitations select Outcome B rather than overstating diagnosis coverage.
