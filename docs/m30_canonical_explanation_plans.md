# M-30 canonical explanation plans

`CONCISE`, `FULL`, and `SOLUTION_AFTER_ATTEMPT` plans must equal the canonical plan generated from the verified graph. Rehashing an omitted, reordered or substituted step cannot make a plan valid.
