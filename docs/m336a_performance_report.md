# M-33.6a performance report

The exact-I16 gate will measure p50/p95/p99, throughput, and peak Python memory for POM parsing, checksum verification, license normalization, SCM-license verification, source correspondence, qualification, denylist lookup, role codec, disclosure extraction, and historical freeze verification on Windows and Karina.

The implementation optimization already changes historical Git blob acquisition from hundreds of subprocesses to one batch process. Correctness and fail-closed behavior remain the priority. Final cross-platform measurements belong to E16 evidence.
