# M-30 public confirmation lifecycle

Public callers receive only an opaque pending handle, action label, summary and expiry. The internal action is bound to learner, conversation, request hash, language and dependency snapshot. Confirmation is exact, expiring and single-use; cancellation is terminal. Internal router proposals are never returned.
