# M-33.6c disclosure registry verification

The append-only disclosed Java registry verifies as a six-entry content-addressed chain. No candidate was removed because its qualification changed.

The registry binds coordinates, Maven archive hashes, POM hashes, source URLs, 1,684 raw hashes, 1,684 canonical hashes, six source-tree hashes, six immutable SCM commits and six correspondence hashes. Declaration fingerprints remain an empty observed class because H17 stopped before the selector/production phase; the empty value is preserved rather than invented.
