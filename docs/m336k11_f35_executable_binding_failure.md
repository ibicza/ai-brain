# M-33.6k.11 F35 executable-binding failure

F35 (`ad5342da01dfa05d690f7341c69e4e0e78f5fa2b`) is an immutable historical `OUTCOME C - BLOCKED`. Its single post-freeze validate-only invocation failed in `executable_dependency_manifest_from_dict` with `M336K2ProtocolError: M336K2 executable manifest is invalid`. No controller, route, acquisition, selector, evaluator, source request, source-body write, vault creation, or reservation mutation followed.

The committed controller executable manifest is `e1c4dc4765c2f980ab29cbb07baa171392bc8ef091a9008d363423e239fedc7f`. It stores `77f06836cbfdd9ac7ba0e1333120b6985f672972a5f018276bb11eeb6c5640d2` in a field named `minimal_environment_policy_hash`; the exact hermetic process recomputed `476f9fb7422f6f8bc644e8266021a50df8ede0f4f65ea765f51f3f7671fa7360` and rejected the manifest.

The exact F35 expression is `content_hash(tuple(sorted(m336k2_minimal_environment())))`. Python sorts the mapping's keys here, not its items. Consequently, the historical identity binds the presence of inherited live-environment names rather than the static rules and does not bind the corresponding values. The build key set is uniquely recoverable from the aggregate hash: it contains `LANG` and `LC_ALL`; the frozen hermetic invocation-plan environment does not. Raw values are neither needed for this proof nor published.

This precise code finding narrows the task's legacy classification `LIVE_ENVIRONMENT_VALUES_HASHED_AS_STATIC_POLICY_IDENTITY`: the unstable input is the effective environment's key-presence set. The architectural failure remains the same—build-process effective state was presented and revalidated as a static policy identity.

F35 also mixed current controller artifacts with copied legacy native artifacts. Its active executable alias is byte-identical to the controller manifest, and its active Python-environment alias is byte-identical to the controller environment. But the copied native route references executable manifest `ed89e40b399bc289516e8363c46a095cf700762f77eff1744941bb766512d8a3` and Python environment `95f1f4a48a71f800387a360c380e1cc8941fd2fe67845d7cba1eca1d1b8effcf`, while the current controller hashes are respectively `e1c4dc4765c2f980ab29cbb07baa171392bc8ef091a9008d363423e239fedc7f` and `e2034b47b953845f80619e0fbfe4e8147c08c8b9e38814ae9aad8265d7f0c46d`. The copied capsule and copied route both retain the hermetic-process key-set hash `476f9fb7422f6f8bc644e8266021a50df8ede0f4f65ea765f51f3f7671fa7360`.

The v4 recovery therefore uses three separate authorities: a static `M336K5PythonStartupPolicy`, a frozen `M336K5SanitizedEnvironment` identity, and an actual `M336K5PythonStartupReceipt`. A canonical v4 controller executable binding produces every active Windows controller/native artifact. Historical loaders remain explicitly read-only, rehearsals remain rehearsal-only, and the persistent Karina capsule remains a separate source domain whose existing liveness and bridge contracts are preserved.

The canonical machine-readable evidence, including the producer/consumer inventory, artifact byte and semantic hashes, mismatch predicates, environment-name presence proof, nine required classifications, and zero side-effect counters, is `artifacts/m336k11/f35-executable-binding-forensics.json`.
