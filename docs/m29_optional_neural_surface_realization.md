# M-29 Optional Neural Surface Realization

No neural surface realizer is enabled in M-29. Deterministic RU/EN rendering meets the integrity gates, so the optional GPU experiment was not needed.

Trusted imports contain no torch/checkpoint/training dependency. Any future paraphraser must remain non-authoritative, preserve every mapped value/unit/formula/citation, pass verifier gates, and fall back to deterministic rendering on failure.
