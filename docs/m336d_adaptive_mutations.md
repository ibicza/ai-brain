# M-33.6d adaptive mutations

The adaptive battery contains 10,260 mutations in 38 required categories. Each mutation changes a protected artifact, recomputes attacker-visible hashes, regenerates role/disclosure/tree/non-authoritative receipt identities, and submits the internally consistent forgery.

Rows bind mutation ID/category, changed artifact, regenerated dependencies, forged hashes, expected and observed rejection layer/code, acceptance state and row hash. Windows and Karina both observed accepted count 0 and wrong rejection layer count 0. Coverage includes authority root/widening/replay, raw/encoded leakage, recursive schema/type/path attacks, selector/seal/order attacks, scoped licensing and SCM correspondence.
