# M-33.6c limitations

M-33.6c is a disclosed-development repair, not a fresh black-box experiment. The six source bodies were already disclosed by H17, so their successful rehearsal cannot be presented as an untouched result.

The frozen matcher supports the five license identifiers required for this repair; a future expansion must freeze additional official templates and independently test them. Source-use receipts encode supplied engineering authority and are not autonomous legal opinions.

The development corpus is intentionally capped at 120 selected files while preserving all six roots. This limits runtime while exercising the exact production, pack, replay, evaluator and installed-runtime path. M-33.6d must perform the next untouched freeze with the overprovisioned metadata-only pool. M-33.7 remains the four-domain proof. Roadmap M-34 Episodic and Relationship Memory has not started.

The required historical Ruff baseline closure is isolated to these 11 format-only paths: `scripts/m343_build_java_corpus.py`, `scripts/m343_pre_freeze_acceptance.py`, `src/ai_brain/stage3/acquisition/java_goldens.py`, `java_pipeline.py`, `java_pre_freeze_gate.py`, `java_process_audit.py`, `java_replay.py`, `java_seal.py`, `src/ai_brain/stage3/knowledge_ir/serialization.py`, `validation.py`, and `tests/test_m343_semantic_proposal_gate.py`. AST comparison against E17 reports zero differences for all 11; no configuration or functional change is hidden in this set.
