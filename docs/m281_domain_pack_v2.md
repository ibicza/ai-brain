# Chemistry Domain Pack V2

Domain 1.1.0/schema 2 bundles `sources`, `fact_memory`, `domain_manifest.json`,
and an initially empty `results` store. The pack contains 33 selected elements,
34 entities, 16 predicates, 430 claims, 100 evidence records, and 9 FactMemory
sources.

The manifest binds source policy 2.0, calculation/rendering 2.0, snapshot/result
schema 2, tool-input policy v2, formula grammar, numeric limits, rounding,
source chain, tool registry, and FactMemory snapshot.

Reproducible content hash:
`58f2f25dd61ffc85b87f6f2fa2e6343286cc0af1056c31d1fb73f984ab4fc2c7`.
Runtime source lookup is pack-relative. A moved pack verifies without global
workspace paths. Domain v1 is rejected with `REBUILD_REQUIRED_FROM_FROZEN_SOURCES`.
