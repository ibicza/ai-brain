# M-30 historical versus current authority

Structural and semantic history verification reconstructs the stored closure using its catalog anchor and receipt structure. Currentness is reported separately. A stale but intact history is `HISTORY_VALID` plus `STALE_WITH_HISTORY_VALID`: it remains inspectable and backup-eligible, but runtime `_load` blocks every new grade, hint, solution or progress observation.
