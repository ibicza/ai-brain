# M-33.6j Karina storage reclamation

The stable Karina identity and SSH host key were verified before inventory or
deletion. The preservation set bound the current R25a worktree and Git object
database, active direct-Python capsule environment, exact JDK, canonical
disclosed vault, current route inputs, and unique evidence.

Two clean registered stale worktrees and four receipt-backed generated SQLite
build-output directories were removed one at a time. A dirty worktree, a
generated tree without its generation receipt, caches, environments, system
data, encrypted filesystems, and every `REVIEW_REQUIRED` or unrelated path were
left untouched.

Root/private-filesystem free space increased from 381,210,624 bytes to
14,218,022,912 bytes. Free inodes after cleanup were 5,962,820. Reclaimed bytes
were 8,274,612,224 from stale worktrees and 5,562,241,024 from generated build
output; cache and system-cache reclamation were both zero. The 12 GiB
qualification target passed with writable, atomic-rename, and fsync probes.

The public preservation-set hash is
`9967e9b883751baa161905a4d66216130f1a648ce3cee547cdcbfa2994001693`.
Encrypted partitions were not mounted or unlocked, unrelated user data was not
deleted, and the active capsule, JDK, current worktree, and canonical disclosed
vault remained verified after cleanup.
